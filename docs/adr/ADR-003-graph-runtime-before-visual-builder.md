# ADR-003: Graph-capable runtime before visual builder

## Status

Accepted

## Decision

Design the workflow runtime as graph-capable before building a visual workflow editor.

## Rationale

Runtime integrity is the hard part. UI convenience must not lock the system into a shallow sequential-only execution model.

