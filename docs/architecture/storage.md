# Storage Guide

SQLite is the default persistence layer.

Primary persisted entities:

- collections
- requests
- environments
- history
- workflows
- export presets

All schema changes require migration support.

