# Functions Guide

Functions are the low-ceremony extension system for SCLPLAPI.

Initial target types:

- `pre_request`
- `post_response`
- `auth_token`

Function discovery is filesystem-based and should remain easy to inspect and hot-reload.

