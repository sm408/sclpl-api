# MVP Requirements

The MVP must include only the smallest coherent platform slice:

- request builder and execution
- collections
- environments
- request history
- CSV batch runner
- filesystem-backed Python function hooks
- JSON/CSV export

The MVP must not require:

- workflow graph UI
- plugin marketplace
- remote sync
- analytics dashboards

