# Runtime Rules

- Runtime before visual builder.
- Strict core contracts before UI acceleration.
- SQLite-first until there is a proven reason to add storage complexity.
- Lightweight event bus, not distributed event theater.
- Python extensibility stays low-ceremony and filesystem-visible.
